// const handleUserMessage = (message, state) => {
//     // Process the user's message here
//     const selectedValue = message; // Assume the message contains the user's selection
  
//     // Respond based on the selected value
//     const response = createChatBotMessage(`You selected: ${selectedValue}`);
//     return response;
//   };
  
//   export default handleUserMessage;